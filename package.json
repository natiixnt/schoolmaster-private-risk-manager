{
  "name": "schoolmaster",
  "private": true,
  "packageManager": "npm@11.6.2",
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "seed": "turbo run seed --filter=api",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.0.3",
    "typescript": "^5.4.5"
  }
}
